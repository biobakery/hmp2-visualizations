HMP2 visualizations. Start up the webapp with `python webapp.py`

update repo

merge / update any local files with local paths etc
rm .doit.db
rm pipeline_state.json
rm -r products

. /seq/ibdmdb/centos6/bin/activate
doit (wait...)
python webapp.py

